
##### CLIENT
-disabled the sync button untill user makes changes(later)
-add a way to remove rows(later)
-feedback when the user adds rows(nah)
-celltemplate https://reactgrid.com/docs/4.0/5-create-your-own-cell-template/#1.defineflagcellinterface (later)
-cap cell count(later)
-remove white space when scrolling fast(later)
-start writing some typescript(later)
##### SERVER
-test in real life setting